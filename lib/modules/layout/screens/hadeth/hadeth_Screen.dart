import 'package:flutter/material.dart';

class hadethSreen extends StatelessWidget {
  const hadethSreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
    );
  }
}
